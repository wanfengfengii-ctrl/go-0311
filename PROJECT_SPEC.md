# 超高层单元式幕墙结构胶粘结证据与吊装准入闭环

## 项目目标

构建一个可编译运行、支持重启恢复的 Go HTTP 后端，围绕“楼栋—立面区—板块—胶缝—连续分段—注胶道次”管理结构硅酮密封胶施工。锁定任务后，系统依次接收清洁、底涂、双组分启封混合、试打、连续注胶、修整封签、养护、随批试验、返工复验、双人复核和终局裁定证据。所有业务变化以持久化只追加事件表达，并在同一事务内更新材料整数毫克账本、限时资源租约、胶缝有效前缀和当前代次投影；服务重启后通过事件与事务日志恢复，不依赖内存状态。建议采用标准库 net/http、关系型数据库兼容的数据访问抽象及 SQLite 默认实现；生产代码控制在20—28个Go文件、约2500有效行、4—6个业务包，公开测试不少于4个文件和12个确定性用例。

## 端到端业务流程

1. 任务锁定：提交设计版本、相容性摘要版本、楼栋与立面区、板块编号、基材和表面处理、结构胶双组分及底涂批次、垫块、胶缝边界与分段、设计宽深比、注胶方向、试打及取样映射、养护计划、承载邻接表、强度阈值和任务代次；一次校验全集后生成不可变快照，陈旧摘要、缺失胶缝、交叠分段或批次错配整体拒绝。
2. 施工准备与混合：合格人员按依赖确认清洁和底涂，随后以操作号原子扣减基胶、固化剂、底涂和试打预算，同时获取计量泵、注胶台及混合器租约并创建材料代次；蝴蝶试验和拉断试验须落在锁定逻辑时点，设备失败只生成待重试调用。
3. 连续注胶与封签：在开放时间和有效租约内，按锁定方向逐段提交注胶道次；每次提交校验前一有效前缀、质量、体积、比例、气泡和断胶记录，成功后才扩展前缀。全部分段闭合后方可修整、封签并转入养护架位。
4. 养护与试验：温湿度探头按逻辑时间写入养护轨迹，系统以确定定点算法计算等效养护时长；达到窗口后消耗随批剥离试件并记录硬度、拉伸强度、伸长率和粘结破坏比例，设备拒绝或超时不得生成替代读数。
5. 异常返工与复验：比例漂移、断胶、气泡、宽深比越界、固化异常、界面破坏或强度不足触发一次性影响传播，按同混合器工作窗口、同材料批次、相邻胶缝及板块承载邻接关系生成排序且唯一的复验集合；割胶记录旧胶体去向并创建新代次，新代次重新覆盖受影响分段和试件，旧证据永久可查。
6. 双人复核与终局：两名不同且具备有效资格的人员分别复核材料去向、闭合前缀、养护时间轴、填充和试验结论；满足全部条件后可申请吊装准入。准入、粘结风险隔离和取消共享单写终局屏障，并发请求最多成功一个，其他请求返回当前终局。

## 核心组件与职责

1. 幕墙胶缝与材料规则目录：保存不可变任务快照，验证设计及相容性版本、基材表面处理、材料批次、胶缝几何、分段覆盖、注胶方向、垫块、试件映射、邻接关系和阈值。
2. 板块注胶任务及证据聚合：实现板块、胶缝、分段、道次和任务代次聚合，以只追加事件和可重建投影维护依赖阶段、连续施工前缀、操作幂等记录及证据查询。
3. 组分守恒与资源租约管理器：维护整数毫克复式质量账本及计量泵、比例监测器、注胶台、混合器、养护架位、拉伸机和割胶工位的限时互斥租约，提供数据库事务内的原子扣减、归集、租约竞争和到期处理。
4. 注胶养护及试验记录器：校验开放时间、设备调用、逻辑时间和定点指标，记录试打、注胶、修整、封签、温湿度轨迹、随批剥离、硬度和拉伸证据，并管理确定重试次数。
5. 返工复验与终局仲裁器：确定性计算异常影响集合，管理割胶重注代次、迟到回执隔离、双人复核以及准入、风险隔离、取消之间的单写终局竞争。
6. Go HTTP API：提供JSON命令与查询端点、稳定错误封装、操作号幂等控制、事务边界、健康检查和重启恢复入口；不包含与质量闭环无关的用户界面或外围系统。

## 领域规则与不变量

1. 任务锁定后，胶缝全集、连续分段、注胶方向、材料批次、试验映射、养护日程、承载邻接关系、阈值及任务代次不可原位修改；变更必须创建新任务代次，已发生证据仍归属旧代次。
2. 胶缝采用整数微米坐标和尺寸。分段必须首尾相接、连续覆盖设计边界且无交叠；宽度、深度、转角、垫块占位和有效粘结面积拒绝负值、退化区间、除零以及有符号64位乘加溢出。
3. 每条胶缝只能按锁定方向扩展连续有效前缀。下一段编号、起点、材料代次或道次不匹配，以及跳段、逆序、断胶、污染混合器、租约或开放时间过期时，不得增加前缀。
4. 所有质量均为整数毫克。输入来源为基胶、固化剂和底涂库存，输出去向为试打料、有效入缝量、试件、割除料、剩余可用料及合理损耗；每个材料代次分别满足输入总量等于输出与余额总量，双组分不可互相抵销。
5. 混合比例、单位长度用量、宽深比、填充率、表干时间、等效养护时长、拉伸强度、伸长率和粘结破坏比例使用文档化缩放因子与整数半向上舍入；先检查符号和时间顺序，再以溢出安全的乘除顺序计算，边界值按锁定规则包含或排除。
6. 资源租约以资源ID、持有操作、取得逻辑时间、截止逻辑时间和令牌标识；同一资源同一时刻只有一个有效令牌。过期令牌不能写证据，释放必须匹配令牌；混合器切换结构胶批次前必须存在完成的清洗证据。
7. 设备拒绝、断连、超时、校准过期或格式错误仅写入带请求摘要、逻辑时间、尝试序号和确定下次尝试时间的待重试记录；同一调用达到锁定重试上限后转为失败证据，不得伪造读数、推进前缀或解除养护屏障。
8. 异常影响集合按故障发生时的不可变关系快照计算并去重；排序键为楼栋、立面区、板块、胶缝、分段、材料代次、复验代次。旧代次的迟到设备回执或检测结果可追加审计事件，但不能改变当前代次结论。
9. 相同操作号与规范化请求摘要相同则返回原结果；内容不同返回幂等冲突。材料扣减、代次创建、租约取得、事件追加和投影更新处于同一事务，任何失败均不得留下部分状态。
10. 板块只有在全部物料及割除物去向守恒、胶缝前缀闭合、养护轨迹无缺口、几何填充和随批试验达标、复验全部关闭且两名不同合格人员独立复核后才能准入；每个任务代次只能存在一个终局。

## 数据模型与持久化

1. DesignLock：任务ID、任务代次、楼栋、立面区、板块、设计版本、相容性摘要版本及有效期、玻璃与铝框表面处理摘要、阈值快照、锁定逻辑时间。
2. JointSpec与SegmentSpec：胶缝ID、方向、设计边界、宽深整数微米、转角、隔离垫块区间、有效粘结面积、连续分段序号、试打及试件映射。
3. MaterialBatch与MaterialGeneration：结构胶基胶批次、固化剂批次、底涂批次、混合器窗口、目标比例、开放截止时间、代次状态；MassEntry记录来源或去向类别、组分、整数毫克和关联证据。
4. EvidenceEvent：单调事件序号、聚合ID、任务及材料/返工代次、事件类型、规范化载荷摘要、逻辑时间、写入时间和前序哈希；JointProjection保存当前阶段、有效连续前缀和当前代次，仅能由事件重建。
5. ResourceLease与DeviceCall：资源类型和ID、租约令牌、持有操作、逻辑起止时间；设备调用包含请求摘要、设备校准状态、尝试次数、重试时间、响应状态和原始格式摘要。
6. CureSample与TestResult：养护架位、温湿度读数序列、等效养护累计值、试件ID及质量、蝴蝶/拉断/剥离/硬度/拉伸结果、定点单位、破坏模式与判定。
7. ReworkCase：异常类别、根证据、唯一影响集合摘要、受影响板块胶缝分段、割除质量及去向、新材料代次、复验代次和关闭状态。
8. Review与TerminalDecision：人员ID、资格快照、独立复核摘要、复核逻辑时间；终局类型为HOIST_ADMITTED、BOND_RISK_ISOLATED或CANCELLED，包含唯一凭据号、前置证据摘要和屏障版本。
9. IdempotencyRecord与TransactionJournal：操作号、端点、请求摘要、确定响应、提交事件范围；事务日志记录预备与提交标记，用于启动时识别并忽略未提交批次。

## 公开接口

1. POST /v1/tasks/lock：校验并锁定板块、胶缝、材料、试验、养护、邻接及阈值全集；返回任务代次和锁定摘要。
2. POST /v1/tasks/{task}/commands：统一提交清洁、底涂、启封混合、试打、逐段注胶、修整、封签、养护读数和检测命令；请求包含operation_id、expected_generation、logical_time及资源租约令牌。
3. POST /v1/leases/acquire与POST /v1/leases/release：在业务事务中申请或释放指定资源租约；材料启封和注胶命令可携带原子租约申请，避免先占料后占设备。
4. POST /v1/device-calls/{call}/results与POST /v1/device-calls/{call}/retry：接收脚本化设备结果或推进确定重试；拒绝不属于当前代次、尝试序号错误或校准无效的回执。
5. POST /v1/tasks/{task}/reworks：基于异常证据生成唯一复验集合；POST /v1/reworks/{id}/cutout和/reinject分别登记割除去向与新代次重注。
6. POST /v1/tasks/{task}/reviews与POST /v1/tasks/{task}/terminal：提交独立复核及终局申请；使用条件更新实现单写屏障，成功准入返回可验证的唯一吊装凭据。
7. GET /v1/tasks/{task}、/evidence、/mass-balance、/reworks和/terminal：按固定排序查询当前投影、完整事件链、组分账本、复验集合及终局；分页游标由稳定排序键组成。
8. 所有错误响应为{"code":"稳定错误码","reasons":[...],"retryable":布尔值}；原因按照楼栋、立面区、板块、胶缝、分段、材料代次、复验代次排序，HTTP状态只表示协议层类别。

## 失败边界

1. 锁定边界：设计或相容性摘要陈旧、胶缝缺失/交叠、几何溢出、材料错配或邻接引用不存在时，整个锁定事务失败并返回如STALE_COMPATIBILITY、JOINT_COVERAGE_INVALID、FIXED_POINT_OVERFLOW。
2. 材料与租约边界：双组分并发超领、底涂不足、资源竞争、混合器跨批污染或租约过期返回MATERIAL_OVERDRAW、LEASE_CONFLICT、MIXER_CONTAMINATED或LEASE_EXPIRED；库存、代次、租约和证据均不得部分提交。
3. 施工状态边界：依赖未满足、开放时间过期、跳段、逆序、错误代次、断胶或气泡返回DEPENDENCY_UNMET、OPEN_TIME_EXPIRED、NONCONTIGUOUS_PREFIX或GENERATION_MISMATCH，有效前缀保持不变。
4. 时间与设备边界：逻辑时间倒退、养护轨迹缺口、设备拒绝/超时/断连、校准过期及格式错误形成确定待重试记录；禁止生成业务读数或释放养护与检测屏障。
5. 返工边界：同一根异常重复触发返回原复验集合；并发创建不同新代次时仅一个提交，失败者返回REWORK_GENERATION_CONFLICT，旧代次证据和物料去向不可覆盖。
6. 幂等与终局边界：同操作号不同内容返回IDEMPOTENCY_CONFLICT；准入、隔离、取消并发仅一个条件写成功，其余返回TERMINAL_ALREADY_DECIDED及既有终局。数据库错误或进程中断只允许恢复已提交事务。

## 验收标准

1. 锁定请求能一次固定板块与胶缝全集、基材及表面处理摘要、结构胶双组分和底涂批次、连续分段、顺序、试验取样映射、养护日程、承载邻接、阈值和任务代次；任何陈旧或不一致输入均无部分记录。
2. 整数微米几何校验可确定验证连续覆盖、无交叠、宽深比、转角、垫块位置和有效粘结面积，并明确拒绝退化区间、负值、除零及64位乘加溢出。
3. 整数毫克账本在并发启封、试打、注胶、试件、割除和损耗下始终保持各组分守恒；扣减、材料代次和资源租约原子提交，相同内容幂等重试稳定返回，不同内容冲突整体回滚。
4. 清洁、底涂、试打、注胶、修整、封签、养护和检测严格按依赖推进；胶缝只形成锁定方向的连续分段前缀，跳段、逆序、污染、错误代次、开放时间或租约过期均不写有效证据。
5. 全部业务指标使用确定整数定点算法，针对比例、用量、宽深比、填充率、表干、等效养护、强度、伸长率和粘结破坏比例的符号、舍入、边界、时间区间及溢出得到跨平台一致结果。
6. 脚本化设备拒绝、断连、超时、校准过期和格式错误只产生带逻辑时间与确定尝试次数的待重试调用；服务重启后可继续重试，且无伪造读数、前缀推进或养护屏障释放。
7. 任一规定异常可按混合器窗口、材料批次、相邻胶缝和承载邻接关系生成唯一、稳定排序的割胶重注复验集合；新代次完整覆盖影响范围，旧证据及迟到回执不会改变当前结论。
8. 仅在守恒、前缀闭合、养护完整、填充与试验达标、复验关闭及两名不同合格人员复核后生成唯一吊装准入凭据；准入、风险隔离与取消并发只能形成一个持久化终局，重启后结论不变。

## 确定性测试场景

1. geometry_test.go：用虚构塔楼A座东立面板块P-017验证三段整数微米胶缝恰好连续覆盖；分别断言间隙、交叠、退化段、负尺寸、除零与乘法溢出的稳定错误码和排序原因。
2. mass_lease_test.go：以固定毫克基胶和固化剂启动两个同步协程竞争同一库存及混合器，断言只有一个原子成功、失败方没有扣料或租约；再验证试打、入缝、试件、割除和损耗的逐组分守恒。
3. workflow_test.go：按逻辑时间完成清洁、底涂、试打和三个连续分段，断言有效前缀逐段增长；并测试跳过第二段、逆向提交、错误材料代次、开放时间边界和过期令牌均不改变前缀。
4. fixedpoint_cure_test.go：以固定温湿度序列验证等效养护、宽深比、填充率、单位长度用量、拉伸强度和破坏比例的精确整数结果，覆盖半向上舍入、恰好阈值、阈值差一和中间值溢出。
5. device_retry_test.go：脚本化比例监测器先超时、再格式错误、最后成功，断言尝试序号和重试逻辑时间固定；校准过期的拉伸机不得消耗试件或解除检测屏障，重启后待重试状态一致。
6. rework_test.go：向共享混合器窗口注入比例漂移，使用公开胶缝邻接和板块承载邻接表断言影响集合唯一且稳定排序；验证割除质量去向、新代次重注、旧代次迟到合格回执隔离。
7. idempotency_tx_test.go：相同操作号及相同规范化内容重复提交得到完全相同响应；同操作号不同质量返回IDEMPOTENCY_CONFLICT；在事件追加前后注入数据库故障，断言无部分扣料、租约、前缀、养护证据或凭据。
8. terminal_race_test.go：使用同步屏障并发发起吊装准入、风险隔离和取消，精确断言仅一个终局提交；缺少第二复核人、复核人为同一人或复验未关闭时准入失败，完成条件后凭据唯一且重启可查。

## 组件追踪关系

1. 规则目录对应验收1、2，负责锁定快照、胶缝覆盖、表面处理、批次、邻接和阈值，是geometry_test.go与锁定接口的主要验证对象。
2. 证据聚合对应验收1、4，负责只追加链、阶段依赖、连续前缀、操作幂等和重启重建，由workflow_test.go及idempotency_tx_test.go覆盖。
3. 守恒与租约管理器对应验收3，负责毫克账本、原子扣减、资源互斥和污染屏障，由mass_lease_test.go及事务故障注入覆盖。
4. 养护及试验记录器对应验收4、5、6，负责定点运算、时间轴、设备调用和确定重试，由fixedpoint_cure_test.go与device_retry_test.go覆盖。
5. 返工复验与终局仲裁器对应验收7、8，负责影响传播、代次隔离、双人复核及单写终局，由rework_test.go与terminal_race_test.go覆盖。
6. HTTP API贯穿全部验收，统一承载命令事务、稳定错误、固定排序和查询恢复；处理器、错误映射、JSON模型、事务中间件和恢复启动逻辑形成独立但紧密协作的Go文件。

## 独特性

本计划专注超高层单元式幕墙结构硅酮密封胶的连续粘结证据、双组分质量守恒、异常割胶重注和板块吊装准入，区别于既有幕墙夹层玻璃装配项目；其核心难点是胶缝方向性连续前缀、混合器窗口传播、材料代次隔离及并发终局，而非玻璃组件装配验收。
